#QUESTÃO 7
numMaior = 0

num = int(input('Digite um número inteiro: '))

while num > 0:

    if num > numMaior:

        numMaior = num

    num = int(input('Digite um número inteiro: '))

print(f'O maior número e: {numMaior}')
